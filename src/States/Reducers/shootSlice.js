import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    selected: Boolean
};


const shootState = createSlice({
    name: "Shoot",
    initialState,
    reducers: {
        
    }
})